---
interface Props {
	tag: string;
	active?: boolean;
	class?: string;
	href?: string;
}

const { tag, active = false, class: className = "", href } = Astro.props;
const Element = href ? "a" : "span";
---

<Element
	href={href}
	class:list={[
		"rounded-full px-3 py-1.5 text-sm font-medium no-underline",
		active
			? "bg-penguin-primary text-penguin-text-dark hover:brightness-110 focus:brightness-110 active:brightness-90"
			: "bg-penguin-accent-light dark:bg-penguin-accent-dark text-penguin-text-light dark:text-penguin-text-dark hover:brightness-90 focus:brightness-90 active:brightness-110 dark:hover:brightness-110 dark:focus:brightness-110 dark:active:brightness-90",
		className,
	]}
>
	{tag}
</Element>
